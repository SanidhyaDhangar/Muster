{
  "name": "muster",
  "private": true,
  "version": "1.0.0",
  "description": "Face-recognition attendance, run entirely on your own machine — FastAPI + React, local OpenCV vision, no paid APIs or cloud uploads.",
  "scripts": {
    "backend": "node scripts/backend.js",
    "frontend": "npm --prefix frontend run dev",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm:backend\" \"npm:frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
